<!DOCTYPE html>
<html>
  <head>
    <title>WEMO</title>
    <style>
      @font-face {
        font-family: "FontAwesome";
        src: url("/fonts/fontawesome-webfont.eot");
        src: url("/fonts/fontawesome-webfont.eot?#iefix") format('eot'), 
             url("/fonts/fontawesome-webfont.woff") format('woff'), 
             url("/fonts/fontawesome-webfont.ttf") format('truetype'), 
             url("/fonts/fontawesome-webfont.svg#FontAwesome") format('svg');
        font-weight: normal;
        font-style: normal;
      }

      body { font: 16px Helvetica; background: #000000; }
      ul li { list-style: none; }
      .device { position: relative; font-family: FontAwesome; width: 100px; height: 150px; background: url("/images/skewed_print.png"); border-radius: 6px; float: left; margin: 0 5px 5px 0; }
      .device .title { display: block; margin-top: 15px; text-align: center; color: #FFFFFF; }

      .device .state { 
        position: absolute; 
        bottom: 20px;
        left: 32px;
        width: 22px; 
        height: 22px; 
        border-radius: 30px; 
        padding: 8px;
        color: rgb(37, 37, 37);
        font-size: 25px;
        background: rgb(83, 87, 93);
        box-shadow: 0px 3px 0px rgb(34, 34, 34),
                    0px 7px 10px 0px rgb(17, 17, 17),
                    inset 0px 1px 1px 0px rgba(250, 250, 250, 0.2),
                    inset 0px -12px 35px 0px rgba(0, 0, 0, 0.5);
      }
    </style>
  </head>
  <body>
    <ul>
      <% devices.each do |device| %>
        <li class="device">
          <strong class="title"><%= device.name %></strong>
          <div class="state <%= device.on? ? "on" : "off" %>">&#xF011;</div>
        </li>
      <% end %>
    </ul>
  </body>
</html>
